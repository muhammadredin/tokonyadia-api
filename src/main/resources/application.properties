spring.application.name=tokonyadia-api
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
server.port=8081
frontend.url=http://localhost:5173

spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:tokonyadia_db}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:Enigma}

tokonyadia.api.jwt-issuer=${JWT_ISSUER:Tokonyadia}
tokonyadia.api.refresh-token-expiration-in-hours=${REFRESH_TOKEN_EXPIRATION_IN_HOURS:24}
tokonyadia.api.password-reset-token-expiration-in-hours=${PASSWORD_RESET_TOKEN_EXPIRATION_IN_HOURS:24}

# Error Config
server.error.include-stacktrace=never

# User Account Config
tokonyadia.user-admin=${USERNAME_ADMIN:admin}
tokonyadia.user-password=${PASSWORD_ADMIN:password}

# Redis Config
spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}
spring.data.redis.password=${REDIS_PASSWORD:}

# Upload file config
spring.servlet.multipart.max-file-size=2MB
spring.servlet.multipart.max-request-size=10MB
tokonyadia.root-file-path=${ROOT_FILE_PATH:/home/enigma/Enigma/Stacks/Java/tokonyadia-api-media}
tokonyadia.product-image-path=${PRODUCT_IMAGE_PATH:/product-images}
tokonyadia.customer-image-path=${CUSTOMER_IMAGE_PATH:/customer-images}
tokonyadia.store-image-path=${STORE_IMAGE_PATH:/store-images}
tokonyadia.file-size=2097152

# Midtrans config
tokonyadia.api.midtrans-server-key=${MIDRANS_SERVER_KEY:SB-Mid-server-7IlHKM_id41tiG7fpO-ySDCN}
tokonyadia.api.midtrans-snap-payment-url=${MIDRANS_SNAP_PAYMENT_URL:https://app.sandbox.midtrans.com/snap/v1/transactions}

# Java Mail Config
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_SMTP_USERNAME:reedzdevtest@gmail.com}
spring.mail.password=${MAIL_SMTP_PASSWORD:zflhglpvxikzeqrf}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required =true
